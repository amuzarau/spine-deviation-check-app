import streamlit as st

st.set_page_config(page_title="Spine Deviation Check App", layout="centered")

st.title("🧍 Приложение для предварительной проверки нарушений формы позвоночника")

st.info(
    "⚠️ Данный инструмент предоставляет **только предварительную оценку осанки** и "
    "НЕ является медицинским диагнозом. "
    "При наличии сомнений или жалоб обязательно обратитесь к врачу-ортопеду."
)

st.markdown("### 📸 Инструкция по съёмке фотографий")
st.markdown("""
- **Вид со спины (обязательно):** стоя прямо, руки расслаблены  
- **Вид сбоку (по желанию):** полностью видна фигура  
- Хорошее освещение, естественная поза  
- Фотографии **не сохраняются**
""")

st.markdown("---")

back_photo = st.file_uploader(
    "Загрузите фото со спины (обязательно)", type=["jpg", "jpeg", "png"]
)

side_photo = st.file_uploader(
    "Загрузите фото сбоку (по желанию)", type=["jpg", "jpeg", "png"]
)

consent = st.checkbox(
    "Я подтверждаю, что являюсь родителем или законным представителем и даю согласие на проведение данной проверки."
)

if st.button("Анализировать"):
    if not consent:
        st.error("Необходимо подтвердить согласие.")
    elif not back_photo:
        st.error("Необходимо загрузить фото со спины.")
    else:
        # ВРЕМЕННЫЙ РЕЗУЛЬТАТ (MVP ШАГ 1)
        st.success("Уровень риска: НИЗКИЙ")
        st.write("- Значительных отклонений осанки не обнаружено.")
        st.write("- Рекомендуется продолжать наблюдение за осанкой.")
        st.info(
            "⚠️ Напоминаем, что это предварительная оценка. При наличии жалоб обратитесь к врачу."
        )
